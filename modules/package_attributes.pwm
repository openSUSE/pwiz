#! /bin/bash

case $1 in
    desc )
	echo "package attributes dialog provider"
	return
	;;
    longdesc )
	echo "This module provides function for basic package attributes, required by most packaging systems."
	return
	;;
    init )
;;
esac
